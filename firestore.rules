rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tesserati/{tesseratoId} {
      allow read, write: if request.auth != null;
      // O più permissivo se vuoi accesso senza auth:
      // allow read: if true;
      // allow write: if request.auth != null;
    }
  }
}
